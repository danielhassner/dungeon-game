public includes images for items
src includes the run files used to start the server and run the website as well as other files used to assist with certain game features
use run.bat the first time around using cmd, it will download the necessary node modules for the server to run, run it again to start the server, copy the link given and paste it into your browser
use run.sh to do the same but with git bash
Enjoy!
